# Week 1, Day 2: Login Page & Basic State Tasks

Keep this file open in a tab to track your progress! Place an `x` in the brackets as you complete each file.

## 1. `src/app/login/page.tsx`
- [ ] Create a `login` folder inside `src/app/`.
- [ ] Create a `page.tsx` file inside that folder.
- [ ] Create and export a `LoginPage` function.
- [ ] Return a simple `<div>`.
- [ ] Add a heading (`<h1>Login</h1>`).
- [ ] Add a form with two inputs (Email, Password) and a Submit button.
- [ ] Style it with Tailwind to look clean and centered.

## 2. `src/components/Navbar.tsx`
- [ ] Add a "Login" link to your navbar if you haven't already.
- [ ] Add a "Dashboard" link to your navbar if you haven't already.

## 3. `src/app/layout.tsx`
- [ ] Ensure `Navbar` and `Footer` are still wrapping all pages.

## 4. `src/app/page.tsx` (Landing Page)
- [ ] Add a "Login" link to your navbar if you haven't already.
- [ ] Add a "Dashboard" link to your navbar if you haven't already.

## 5. `src/app/dashboard/page.tsx` (Dashboard)
- [ ] Add a "Login" link to your navbar if you haven't already.
- [ ] Add a "Dashboard" link to your navbar if you haven't already.
